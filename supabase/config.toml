# Supabase project configuration.
# Push auth settings to the live project with: supabase config push

project_id = "calendarsync"

[auth]
site_url = "https://dominictatatakis.github.io/CalendarSync"
additional_redirect_urls = [
  "https://dominictatatakis.github.io/CalendarSync/**",
  "http://localhost:8081/**",
]

# NOTE: custom email templates require a paid plan or custom SMTP provider.
# The default email sends a magic link, which the web app supports
# (detectSessionInUrl picks up the session on redirect). If you add SMTP later,
# re-enable this block to send 6-digit codes instead:
# [auth.email.template.magic_link]
# subject = "Your CalendarSync sign-in code"
# content_path = "./supabase/templates/magic_link.html"
